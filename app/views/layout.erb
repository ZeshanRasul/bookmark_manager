<!DOCTYPE html>
<html>
  <head>
    <title> Bookmark Manager</title>
  </head>

  <body>
    <% if current_user %>
      <h2> Welcome <%= current_user.name %> </h2>
    <% end %>

    <% if flash[:error] %>
     <div id="notice"><%= flash[:error] %></div>
    <% end %>

     <%= yield %>
  </body>
</html>
